# transformer_implementation

Implementation of the "Attention is All You Need" paper which describes the transformer architecture.

Inside transformer.py, my implementation of the transformer begins at the Positional Encoding class.

Inside train_logs.txt, you can see the progression of the model over 500 epochs, as it is trained to translate from English to French. The value right below "Epoch {i}" is the loss for that epoch.
